# Telegram Бот для учёта рабочего времени

Этот бот помогает вести учёт рабочего времени, управлять рабочими местами и генерировать отчёты. Он разработан с использованием Python 3.11 и асинхронного фреймворка aiogram.

## Основные возможности

### 📝 Учёт рабочего времени
- Добавление записей о рабочем времени
- Выбор рабочего места
- Указание времени начала и окончания работы
- Добавление описания выполненной работы
- Автоматические напоминания о незавершенных записях

### 🏢 Управление рабочими местами
- Создание новых рабочих мест
- Установка почасовой ставки
- Редактирование и удаление рабочих мест
- Автоматическая очистка неиспользуемых рабочих мест

### 📊 Генерация отчётов
- Отчёты за день/неделю/месяц
- Отчёты за произвольный период
- Группировка по рабочим местам
- Расчёт заработка
- Автоматическая отправка еженедельных отчётов

### ⚙️ Персональные настройки
- Настройка часового пояса
- Определение часового пояса по геолокации
- Управление уведомлениями
- Персонализация форматов отображения

### 🔄 Автоматизация
- Ежедневные напоминания о незавершенных записях (20:00)
- Еженедельные отчеты (воскресенье, 23:00)
- Ежемесячная очистка старых данных (1 число, 02:00)
- Ежедневное резервное копирование (03:00)

## Технический стек

### Основные компоненты
- Python 3.11
- aiogram 2.25.1 (Telegram Bot API)
- Tortoise ORM 0.19.0 (PostgreSQL)
- APScheduler 3.9.1 (фоновые задачи)
- Pydantic 1.10.7 (валидация данных)

### Инфраструктура
- Docker (контейнеризация)
- Railway (хостинг)
- Supabase (база данных PostgreSQL)
- GitHub Actions (CI/CD)

### Тестирование
- pytest (unit и integration тесты)
- pytest-asyncio (асинхронные тесты)
- pytest-cov (измерение покрытия)
- flake8 (линтер)
- black (форматирование кода)

## Установка и запуск

### Локальная разработка

1. Клонируйте репозиторий:
   ```bash
   git clone https://github.com/GrggrT/wh2.git
   cd wh2
   ```

2. Создайте виртуальное окружение и активируйте его:
   ```bash
   python -m venv venv
   source venv/bin/activate  # для Linux/macOS
   venv\Scripts\activate     # для Windows
   ```

3. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```

4. Создайте файл .env с необходимыми переменными окружения:
   ```
   # Telegram Bot Token
   BOT_TOKEN=your_bot_token
   
   # Admin Telegram ID
   ADMIN_ID=your_admin_id
   
   # TimezoneDB API Key
   TIMEZONE_DB_API_KEY=your_timezone_db_api_key
   
   # Database Configuration
   DB_USER=your_db_user
   DB_PASSWORD=your_db_password
   DB_HOST=your_db_host
   DB_PORT=your_db_port
   DB_NAME=your_db_name
   
   # Full Database URL
   DATABASE_URL=postgresql://user:pass@host:port/dbname
   
   # Logging Level
   LOG_LEVEL=INFO
   ```

5. Запустите бота:
   ```bash
   python -m app.bot
   ```

### Запуск через Docker

1. Соберите Docker-образ:
   ```bash
   docker build -t timetracker-bot .
   ```

2. Запустите контейнер:
   ```bash
   docker run -d --env-file .env timetracker-bot
   ```

## Тестирование

### Запуск тестов
```bash
# Запуск всех тестов
pytest

# Запуск с отчетом о покрытии
pytest --cov=app --cov-report=term-missing

# Запуск конкретного теста
pytest tests/test_models.py -v
```

### Проверка кода
```bash
# Проверка стиля кода
flake8 app

# Форматирование кода
black app
```

## Структура проекта

```
project/
├── app/
│   ├── __init__.py
│   ├── bot.py              # Основной файл бота
│   ├── handlers/           # Обработчики команд
│   │   ├── start.py
│   │   ├── add_record.py
│   │   ├── workplaces.py
│   │   ├── reports.py
│   │   └── settings.py
│   ├── middlewares/        # Middleware
│   │   ├── error_handler.py
│   │   └── rate_limiter.py
│   ├── db/                 # Работа с базой данных
│   │   └── models.py
│   └── utils/             # Утилиты
│       ├── validators.py
│       ├── scheduler.py
│       └── health_check.py
├── tests/                 # Тесты
│   ├── test_models.py
│   ├── test_handlers.py
│   ├── test_middleware.py
│   └── test_scheduler.py
├── Dockerfile
├── docker-compose.yml
├── requirements.txt
├── pytest.ini
├── .flake8
└── README.md
```

## Команды бота

### Основные команды
- `/start` - Начало работы с ботом
- `/help` - Показать список команд
- `/add_record` - Добавить запись о рабочем времени
- `/workplaces` - Управление рабочими местами
- `/reports` - Генерация отчётов
- `/settings` - Персональные настройки

### Управление рабочими местами
- `/add_workplace` - Добавить новое рабочее место
- `/edit_workplace` - Редактировать существующее место
- `/delete_workplace` - Удалить рабочее место

### Отчёты
- `/report_today` - Отчёт за сегодня
- `/report_week` - Отчёт за неделю
- `/report_month` - Отчёт за месяц
- `/report_custom` - Отчёт за произвольный период

## Разработка

### Добавление новых команд

1. Создайте новый файл в директории `app/handlers/`
2. Определите обработчики команд
3. Зарегистрируйте обработчики в функции `register_handlers`
4. Импортируйте и добавьте регистрацию в `app/bot.py`

### Миграции базы данных

Для управления миграциями используется Aerich:

```bash
# Инициализация
aerich init -t app.db.models

# Создание миграции
aerich migrate

# Применение миграций
aerich upgrade
```

## CI/CD

Проект использует GitHub Actions для автоматизации:

1. При каждом push в ветку `master`:
   - Запускаются все тесты
   - Проверяется стиль кода
   - Измеряется покрытие тестами
   - Собирается Docker-образ
   - Выполняется деплой на Railway

2. При создании pull request:
   - Запускаются тесты
   - Проверяется стиль кода
   - Измеряется покрытие тестами

## Мониторинг

### Логирование
- Все логи доступны через Railway Dashboard
- Уровень логирования настраивается через `LOG_LEVEL`
- Критические ошибки отправляются администратору

### Проверка работоспособности
- Health-check endpoint проверяет:
  - Подключение к базе данных
  - Работоспособность Telegram API
  - Состояние планировщика задач

### Резервное копирование
- Ежедневное резервное копирование базы данных
- Хранение бэкапов за последние 7 дней
- Автоматическая очистка старых бэкапов

## Лицензия

MIT License. См. файл LICENSE для деталей.

## Поддержка

При возникновении проблем:
1. Проверьте раздел Issues
2. Создайте новый Issue с описанием проблемы
3. Свяжитесь с разработчиками 